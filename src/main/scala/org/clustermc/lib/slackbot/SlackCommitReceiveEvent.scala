package org.clustermc.lib.slackbot

/*
 * Copyright (C) 2013-Current Carter Gale (Ktar5) <buildfresh@gmail.com>
 * 
 * This file is part of Hub.
 * 
 * Hub can not be copied and/or distributed without the express
 * permission of the aforementioned owner.
 */

object SlackCommitReceiveEvent {

  /*implicit val system = ActorSystem("slack")

  val token = "<Your Token Here>"
  val client = Option(null)//SlackRtmClient(token)

  client.onMessage(m => {
    if(m.channel == "dev-changelog"){
      if(m.text.contains("#important")){
        //ClusterLib.instance.slackClient.postChatMessage("punishments", )
      }

      /**
        * channelId: String, text: String, username: Option[String] = None, asUser: Option[Boolean] = None,
      parse: Option[String] = None, linkNames: Option[String] = None, attachments: Option[Seq[Attachment]] = None,
      unfurlLinks: Option[Boolean] = None, unfurlMedia: Option[Boolean] = None, iconUrl: Option[String] = None,
      iconEmoji: Option[String] = None)(implicit ec: ExecutionContext
        */
    }
  })
*/
}
